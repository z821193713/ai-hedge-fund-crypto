# 百炼（阿里云DashScope）配置说明

## 1. 环境变量设置

由于项目使用环境变量来管理API密钥，你需要设置环境变量。有两种方式：

### 方式1：创建.env文件（推荐）

在项目根目录创建`.env`文件，内容如下：

```bash
# 百炼（阿里云DashScope）API配置
DASHSCOPE_API_KEY=sk-45b3ed97e2f34ce8b3e7aa7378f73d7b
```

### 方式2：直接在终端设置

```bash
export DASHSCOPE_API_KEY=sk-45b3ed97e2f34ce8b3e7aa7378f73d7b
```

## 2. 配置文件说明

项目已经配置好了百炼的相关设置：

- `provider: "dashscope"` - 使用百炼服务
- `base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"` - 百炼API地址
- `name: "qwen-plus"` - 使用通义千问Plus模型

## 3. 运行项目

设置好环境变量后，直接运行：

```bash
python main.py
```

或者运行回测：

```bash
python backtest.py
```

## 4. 注意事项

- 确保你的百炼API密钥有效且有足够的额度
- 百炼使用OpenAI兼容模式，所以代码无需大改
- 如果遇到API调用问题，请检查网络连接和API密钥是否正确

## 5. 支持的模型

百炼支持多种模型，你可以在配置文件中修改`name`字段：

- `qwen-plus` - 通义千问Plus（推荐）
- `qwen-turbo` - 通义千问Turbo
- `qwen-max` - 通义千问Max
- `qwen-max-longcontext` - 通义千问Max长文本版 