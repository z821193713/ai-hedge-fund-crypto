# AI Hedge Fund Crypto 完整使用手册

## 📚 目录
1. [项目介绍](#项目介绍)
2. [系统架构](#系统架构)
3. [环境搭建](#环境搭建)
4. [配置说明](#配置说明)
5. [使用教程](#使用教程)
6. [策略开发](#策略开发)
7. [性能优化](#性能优化)
8. [故障排除](#故障排除)
9. [最佳实践](#最佳实践)

## 📖 项目介绍

### 什么是 AI Hedge Fund Crypto？

AI Hedge Fund Crypto 是一个基于人工智能的加密货币对冲基金交易系统，它结合了：

- **图工作流架构**：使用 LangGraph 构建有向无环图工作流
- **多策略集成**：支持 MACD、RSI 等多种技术分析策略
- **AI 智能决策**：集成多种大语言模型进行交易决策
- **多时间框架分析**：同时分析多个时间周期
- **完整回测系统**：详细的历史数据验证

### 核心优势

✅ **模块化设计**：策略、数据、风险管理完全解耦  
✅ **AI 增强**：结合技术分析和 AI 推理  
✅ **高度可配置**：无需修改代码即可调整策略  
✅ **多时间框架**：提高信号质量和稳定性  
✅ **完整生态**：从数据到决策的完整链路  

## 🏗️ 系统架构

### 整体架构

```
用户层 (配置文件、环境变量)
    ↓
应用层 (backtest.py、main.py、Agent)
    ↓
工作流引擎层 (LangGraph - StartNode → DataNodes → StrategyNodes → RiskNode → PortfolioNode)
    ↓
策略层/数据层 (MACD、RSI策略 | Binance API、缓存)
    ↓
AI决策层/风险管理层 (OpenAI、Claude等 | 仓位管理、风险控制)
    ↓
输出层 (交易信号、性能报告、可视化图表)
```

### 核心组件

| 组件 | 功能 | 技术实现 |
|------|------|----------|
| **StartNode** | 工作流初始化 | LangGraph 节点 |
| **DataNode** | 多时间框架数据处理 | Binance API + Pandas |
| **StrategyNode** | 技术分析策略执行 | 自定义策略类 |
| **RiskManagementNode** | 风险评估和控制 | 仓位管理算法 |
| **PortfolioManagementNode** | AI 驱动的最终决策 | LLM + LangChain |

## 🛠️ 环境搭建

### 系统要求

- **操作系统**：Windows 10+、macOS 10.15+、Linux (Ubuntu 18+)
- **Python 版本**：3.12+ (推荐 3.12)
- **内存**：8GB+ (推荐 16GB)
- **存储**：2GB+ 可用空间
- **网络**：稳定的互联网连接

### 安装步骤

#### 1. 克隆项目
```bash
git clone https://github.com/51bitquant/ai-hedge-fund-crypto.git
cd ai-hedge-fund-crypto
```

#### 2. 创建虚拟环境
```bash
# 使用 uv (推荐)
uv venv --python 3.12
source .venv/bin/activate  # Linux/Mac
# 或 .venv\Scripts\activate.bat  # Windows

# 或使用 conda
conda create -n ai-hedge python=3.12
conda activate ai-hedge

# 或使用 venv
python -m venv ai-hedge
source ai-hedge/bin/activate  # Linux/Mac
# 或 ai-hedge\Scripts\activate.bat  # Windows
```

#### 3. 安装依赖
```bash
# 使用 uv (推荐)
uv pip sync

# 或使用 pip
pip install -r requirements.txt
```

#### 4. 验证安装
```bash
python -c "import pandas, numpy, langchain; print('安装成功!')"
```

## ⚙️ 配置说明

### API 密钥配置

#### 1. 复制环境变量文件
```bash
cp .env.example .env
```

#### 2. 编辑 .env 文件
```env
# Binance API (必需 - 用于获取市场数据)
BINANCE_API_KEY=your_binance_api_key
BINANCE_API_SECRET=your_binance_secret

# LLM API 密钥 (选择一个或多个)
OPENAI_API_KEY=your_openai_key          # GPT-4, GPT-4o-mini
GROQ_API_KEY=your_groq_key             # Llama 模型 (快速)
ANTHROPIC_API_KEY=your_anthropic_key    # Claude 模型
GOOGLE_API_KEY=your_google_key         # Gemini 模型
```

#### 3. 获取 API 密钥指南

**Binance API**：
1. 登录 [Binance](https://www.binance.com)
2. 进入 API 管理页面
3. 创建新的 API 密钥
4. 启用 "读取" 权限（数据获取）
5. 如需实盘交易，启用 "交易" 权限

**OpenAI API**：
1. 访问 [OpenAI Platform](https://platform.openai.com/)
2. 创建账户并充值
3. 生成 API 密钥

**其他 LLM 提供商**：
- [Groq](https://console.groq.com/) - 免费额度，速度快
- [Anthropic](https://console.anthropic.com/) - Claude 模型
- [Google AI](https://makersuite.google.com/) - Gemini 模型

### 系统配置

#### 1. 复制配置文件
```bash
cp config.example.yaml config.yaml
```

#### 2. 配置文件详解
```yaml
# 基础设置
mode: backtest              # 运行模式: backtest(回测) 或 live(实时)
start_date: 2025-01-01      # 回测开始日期
end_date: 2025-02-01        # 回测结束日期
primary_interval: 1h        # 主要时间框架
initial_cash: 100000        # 初始资金 (美元)
margin_requirement: 0.0     # 保证金要求 (0.0-1.0)

# 信号配置
signals:
  intervals: ["30m", "1h", "4h"]        # 分析时间框架
  tickers: ["BTCUSDT", "ETHUSDT"]       # 交易对
  strategies: ['MacdStrategy']           # 使用的策略

# AI 模型配置
model:
  name: "gpt-4o-mini"       # 模型名称
  provider: "openai"        # 提供商
  # base_url: "https://api.openai.com/v1"  # 可选：自定义 API 地址

# 显示设置
show_reasoning: false       # 是否显示 AI 推理过程
show_agent_graph: true      # 是否显示工作流图
```

## 🚀 使用教程

### 快速开始 (3 分钟)

```bash
# 1. 确保环境已配置
source .venv/bin/activate

# 2. 运行回测
uv run backtest.py

# 3. 查看结果
# 控制台会显示性能指标
# imgs/ 目录会生成可视化图表
```

### 回测模式详解

#### 1. 基础回测
```bash
# 使用默认配置运行回测
uv run backtest.py
```

#### 2. 回测结果解读
```
=== 回测性能报告 ===
总收益率: 15.23%           # 总体收益
年化收益率: 18.45%         # 年化收益
夏普比率: 1.45            # 风险调整收益 (>1 较好)
最大回撤: -8.12%          # 最大亏损幅度
胜率: 62.5%              # 盈利交易占比
总交易次数: 48            # 交易频率
平均持仓时间: 2.3天       # 持仓周期
```

### 实时模式详解

#### 1. 信号生成模式
```yaml
# config.yaml
mode: live
```

```bash
# 运行实时分析
uv run main.py
```

输出示例：
```json
{
  "decisions": {
    "BTCUSDT": {
      "action": "buy",
      "quantity": 0.1,
      "confidence": 75,
      "reasoning": "MACD 金叉，RSI 从超卖区域反弹，技术面偏多"
    },
    "ETHUSDT": {
      "action": "hold",
      "quantity": 0,
      "confidence": 45,
      "reasoning": "信号混合，短期震荡，建议观望"
    }
  }
}
```

## 🧠 策略开发

### 创建自定义策略

#### 1. 策略文件结构
```python
# src/strategies/my_custom_strategy.py
from typing import Dict, Any
import json
import pandas as pd
from langchain_core.messages import HumanMessage
from src.graph import AgentState, BaseNode

class MyCustomStrategy(BaseNode):
    def __call__(self, state: AgentState) -> Dict[str, Any]:
        """自定义策略实现"""
        data = state.get("data", {})
        data['name'] = "MyCustomStrategy"
        
        tickers = data.get("tickers", [])
        intervals = data.get("intervals", [])
        
        technical_analysis = {}
        for ticker in tickers:
            technical_analysis[ticker] = {}
            for interval in intervals:
                df = data.get(f"{ticker}_{interval.value}", pd.DataFrame())
                
                if df.empty:
                    continue
                
                # 实现自定义分析逻辑
                signal_result = self.analyze_data(df)
                
                technical_analysis[ticker][interval.value] = {
                    "signal": signal_result["direction"],
                    "confidence": signal_result["confidence"],
                    "strategy_signals": {
                        "my_custom_indicator": signal_result
                    }
                }
        
        message = HumanMessage(
            content=json.dumps(technical_analysis),
            name="my_custom_strategy_agent",
        )
        
        state["data"]["analyst_signals"]["my_custom_strategy_agent"] = technical_analysis
        
        return {
            "messages": [message],
            "data": data,
        }
    
    def analyze_data(self, df: pd.DataFrame) -> Dict:
        """自定义分析逻辑"""
        if df.empty:
            return {"direction": "neutral", "confidence": 0}
        
        # 示例：双移动平均线策略
        df['sma_20'] = df['close'].rolling(20).mean()
        df['sma_50'] = df['close'].rolling(50).mean()
        
        if df['sma_20'].iloc[-1] > df['sma_50'].iloc[-1]:
            return {"direction": "bullish", "confidence": 70}
        else:
            return {"direction": "bearish", "confidence": 70}
```

#### 2. 注册策略
```python
# src/strategies/__init__.py
from .my_custom_strategy import MyCustomStrategy

__all__ = [
    "MacdStrategy",
    "RSIStrategy", 
    "MyCustomStrategy",
]
```

#### 3. 配置使用
```yaml
# config.yaml
signals:
  strategies: ['MyCustomStrategy']
```

### 常用技术指标

#### 移动平均线
```python
# 简单移动平均
df['sma_20'] = df['close'].rolling(20).mean()

# 指数移动平均
df['ema_20'] = df['close'].ewm(span=20).mean()
```

#### RSI 指标
```python
def calculate_rsi(df, period=14):
    delta = df['close'].diff()
    gain = (delta.where(delta > 0, 0)).rolling(window=period).mean()
    loss = (-delta.where(delta < 0, 0)).rolling(window=period).mean()
    rs = gain / loss
    rsi = 100 - (100 / (1 + rs))
    return rsi
```

#### MACD 指标
```python
def calculate_macd(df, fast=12, slow=26, signal=9):
    ema_fast = df['close'].ewm(span=fast).mean()
    ema_slow = df['close'].ewm(span=slow).mean()
    macd = ema_fast - ema_slow
    signal_line = macd.ewm(span=signal).mean()
    histogram = macd - signal_line
    return macd, signal_line, histogram
```

## ⚡ 性能优化

### 提高运行速度

#### 1. 使用更快的 LLM
```yaml
# 使用 Groq (速度最快)
model:
  name: "llama-3.1-70b-versatile"
  provider: "groq"
```

#### 2. 减少分析复杂度
```yaml
# 减少时间框架
signals:
  intervals: ["1h"]  # 只使用一个时间框架

# 减少交易对
signals:
  tickers: ["BTCUSDT"]  # 只分析一个交易对
```

#### 3. 启用缓存
```python
# 数据会自动缓存到 cache/ 目录
# 重复运行时会使用缓存数据
```

### 提高策略质量

#### 1. 多时间框架分析
```yaml
signals:
  intervals: ["15m", "1h", "4h", "1d"]  # 多个时间周期
```

#### 2. 多策略组合
```yaml
signals:
  strategies: ['MacdStrategy', 'RSIStrategy']  # 组合多个策略
```

#### 3. 调整策略权重
```python
# 在策略中调整权重
strategy_weights = {
    "trend": 0.30,        # 增加趋势权重
    "momentum": 0.30,     # 增加动量权重
    "mean_reversion": 0.20,
    "volatility": 0.20,
}
```

## 🔧 故障排除

### 常见问题

#### 1. API 连接失败
```
错误: ConnectionError: Unable to connect to Binance API
```

**解决方案**：
- 检查网络连接
- 验证 API 密钥是否正确
- 确认 API 权限设置
- 检查防火墙设置

#### 2. 依赖安装失败
```
错误: Failed to install requirements
```

**解决方案**：
```bash
# 更新 pip
pip install --upgrade pip

# 使用国内镜像
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple/

# 或使用 conda
conda install pandas numpy matplotlib
```

#### 3. 内存不足
```
错误: MemoryError: Unable to allocate array
```

**解决方案**：
- 减少分析的时间范围
- 减少交易对数量
- 增加系统内存
- 使用更小的时间框架

#### 4. LLM API 调用失败
```
错误: OpenAI API rate limit exceeded
```

**解决方案**：
- 检查 API 余额
- 降低调用频率
- 切换到其他 LLM 提供商
- 使用本地模型 (Ollama)

### 调试模式

#### 启用详细日志
```yaml
# config.yaml
show_reasoning: true    # 显示 AI 推理过程
show_agent_graph: true  # 显示工作流图
```

#### 检查数据
```python
# 在策略中添加调试信息
print(f"数据形状: {df.shape}")
print(f"最新价格: {df['close'].iloc[-1]}")
print(f"数据时间范围: {df.index[0]} 到 {df.index[-1]}")
```

## 🎯 最佳实践

### 策略开发

#### 1. 先回测再实盘
```bash
# 充分回测验证
uv run backtest.py

# 确认策略有效后再实时运行
uv run main.py
```

#### 2. 多时间框架验证
```yaml
# 使用多个时间框架提高信号质量
signals:
  intervals: ["30m", "1h", "4h"]
```

#### 3. 合理设置参数
```yaml
# 保守的初始设置
initial_cash: 10000      # 小额资金开始
margin_requirement: 0.5  # 较高保证金要求
```

### 风险管理

#### 1. 设置止损止盈
```python
# 在策略中实现止损逻辑
def check_stop_loss(current_price, entry_price, stop_loss_pct=0.05):
    if current_price < entry_price * (1 - stop_loss_pct):
        return True
    return False
```

#### 2. 分散投资
```yaml
# 投资多个交易对分散风险
signals:
  tickers: ["BTCUSDT", "ETHUSDT", "ADAUSDT", "SOLUSDT"]
```

#### 3. 定期评估
```bash
# 定期运行回测评估策略表现
# 根据市场变化调整参数
```

### 系统运维

#### 1. 定期更新
```bash
# 更新依赖包
uv pip sync --upgrade

# 更新项目代码
git pull origin main
```

#### 2. 监控系统
```bash
# 监控日志文件
tail -f logs/system.log

# 监控系统资源
htop
```

#### 3. 备份配置
```bash
# 备份重要配置文件
cp config.yaml config_backup_$(date +%Y%m%d).yaml
cp .env .env_backup_$(date +%Y%m%d)
```

## 🚨 风险提示

⚠️ **重要警告**：

1. **教育用途**：本系统主要用于学习和研究
2. **投资风险**：加密货币投资存在高风险
3. **资金安全**：请使用您能承受损失的资金
4. **策略验证**：充分回测后再使用实盘资金
5. **市场变化**：策略可能因市场变化而失效
6. **技术风险**：软件可能存在 bug 或故障

## 📚 更多资源

- **[项目 GitHub](https://github.com/51bitquant/ai-hedge-fund-crypto)**
- **[完整架构文档](AI_Hedge_Fund_Crypto_架构文档.md)**
- **[架构图文本版](架构图_文本版.md)**
- **[快速使用指南](快速使用指南.md)**
- **[项目总结](项目总结.md)**

## 🎉 结语

恭喜您完成了 AI Hedge Fund Crypto 的完整学习！这个系统为您提供了：

- 🤖 **AI 驱动的交易决策**
- 📊 **多策略技术分析**
- 🔄 **完整的回测验证**
- ⚙️ **高度可配置的架构**
- 🛡️ **完善的风险管理**

现在您可以开始您的量化交易之旅了！记住：**从小额开始，持续学习，谨慎投资**。

---

**免责声明**：本文档仅供教育和研究使用，不构成投资建议。投资有风险，入市需谨慎。